import {
  Controller,
  // Param,
  // Body,
  // Get,
  // Req,
  // QueryParam,
  // Res,
  // Post,
  // Put,
  // Delete,
} from 'routing-controllers';

@Controller()
export class UserController {
  // @Get('/users/:id')
  // public getAll(@Param("id") id: number, @Req() request: any, @Res() response: any) {
  //   console.log(id)
  //   return response.send('Hello response!');
  // }
  // @Get("/users")
  // public getUsers(@QueryParam("limit") limit: number, @Req() request: any, @Res() response: any) {
  //   console.log(limit)
  //   return response.status(200).json({
  //     malik:'aslşdfkjasdf'
  //   })
  // }
  // @Get('/users/:id')
  // public getOne(@Param('id') id: number) {
  //   return 'This action returns user #' + id;
  // }
  // @Post('/users')
  // public post(@Body() user: any) {
  //   return 'Saving user...';
  // }
  // @Put('/users/:id')
  // public put(@Param('id') id: number, @Body() user: any) {
  //   return 'Updating a user...';
  // }
  // @Delete('/users/:id')
  // public remove(@Param('id') id: number) {
  //   return 'Removing user...';
  // }
}
